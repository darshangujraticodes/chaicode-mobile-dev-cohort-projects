import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    backgroundColor: "#fff",
    padding: 20,
    paddingTop: 50,
    gap: 20,
  },

  heading: {
    fontSize: 25,
    textAlign: "center",
  },
});
